<div class="container-fluid bg-light py-5">
  <!-- HERO -->
  <section class="container py-4" style="background: linear-gradient(180deg, rgba(25,135,84,0.15) 100%, rgba(25, 134,84,1) 0%);">
    <div class="row align-items-center">
      <div class="col-lg-7 text-center text-lg-start">
        <h1 class="display-4 fw-bold mb-3">
          Stay ahead of medical checkups & vaccinations.
        </h1>
        <p class="lead mb-4">
          MediMinder tracks preventive checkups and vaccines for you – so you never miss what matters.
        </p>

        <div class="d-flex flex-column flex-sm-row gap-2 justify-content-center justify-content-lg-start">
          <% if user_signed_in? && current_user.profile.present? %>
            <%= link_to "View your upcoming checkups", root_path, class: "btn btn-primary btn-lg" %>
          <% elsif user_signed_in? %>
            <%= link_to "Create your health profile", root_path, class: "btn btn-primary btn-lg" %>
          <% else %>
            <%= link_to "Get started", new_user_registration_path, class: "btn btn-primary btn-lg" %>
            <%= link_to "Log in", new_user_session_path, class: "btn btn-outline-secondary btn-lg" %>
          <% end %>
        </div>
      </div>

      <div class="col-lg-5 mt-4 mt-lg-0">
        <div class="bg-white border rounded-3 shadow-sm p-4">
          <p class="text-muted small mb-3 text-uppercase">
            MediMinder keeps an eye on
          </p>
          <ul class="list-unstyled mb-0">
            <li class="d-flex align-items-center mb-2">
              <i class="bi bi-calendar-check me-2"></i>
              Age-based preventive checkups
            </li>
            <li class="d-flex align-items-center mb-2">
              <i class="bi bi-syringe me-2"></i>
              Recommended vaccinations
            </li>
            <li class="d-flex align-items-center">
              <i class="bi bi-bell me-2"></i>
              Smart reminders before you’re due
            </li>
          </ul>
        </div>
      </div>
    </div>
  </section>
</div>

<!-- SHORT "WHY" SECTION -->
<section class="py-5">
  <div class="container">
    <div class="row justify-content-center text-center">
      <div class="col-lg-8">
        <h2 class="fw-bold mb-3">Why MediMinder?</h2>
        <p class="lead mb-4">
          Busy life, clear health plan. MediMinder turns guidelines into simple to-do’s –
          and reminds you exactly when checkups and vaccines are due.
        </p>
      </div>
    </div>

    <div class="row text-center g-4 mt-2">
      <div class="col-md-4">
        <i class="bi bi-clock-fill fs-1 mb-2"></i>
        <h5 class="fw-bold">Always on time</h5>
        <p class="mb-0">Automatic reminders before every important checkup.</p>
      </div>
      <div class="col-md-4">
        <i class="bi bi-person-badge-fill fs-1 mb-2"></i>
        <h5 class="fw-bold">Built around you</h5>
        <p class="mb-0">Recommendations based on age and profile data.</p>
      </div>
      <div class="col-md-4">
        <i class="bi bi-shield-check fs-1 mb-2"></i>
        <h5 class="fw-bold">Peace of mind</h5>
        <p class="mb-0">Know that you’re up to date – without spreadsheets or notes.</p>
      </div>
    </div>
  </div>
</section>
